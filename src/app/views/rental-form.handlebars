<h1>Rental Vehicles Web-App</h1>

<div id="main-text">
    Welcome to the Genesis™ Rental Vehicles Web-App. Please select the vehicle you'd like to rent out along
    with the customer you'd like to rent it to.
</div>

<form id="rental-form" method="post" action="/">
    <div class="selection">
        <label for="vehicle-dropdown">Vehicle</label>
        <select id="vehicle-dropdown" name="vehicleid">
            <option value="default">Select a vehicle</option>
            {{#each @vehicles}}
                <option value="{{vehicleid}}">{{vehicleid}} ({{licenseplate}})</option>
            {{/each}}
        </select>

        <label for="customer-dropdown">Customer</label>
        <select id="customer-dropdown" name="customerid">
            <option value="default">Select a customer</option>
            {{#each @customers}}
                <option value="{{customerid}}">{{customerid}} ({{firstname}} {{lastname}})</option>
            {{/each}}
        </select>

        <input type="submit" id="submit-rental" value="Submit"/>
        {{>hint}}
    </div>
</form>